import User from"./components/user";
//JSX
function App() {
  return (
  <div>
    <h1>User</h1>
    <User name="Bhavin" description="FSWD" />
  </div>
  );
};

export default App;

// Props - A data that is being passed from one component to another.
// state - A set of data that an individual component holds.
